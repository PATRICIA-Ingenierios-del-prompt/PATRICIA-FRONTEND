# Copy to `.env` and fill in. `.env` is gitignored (never commit secrets).

# Gateway origin the Vite dev proxy forwards /api, /auth, /ws to.
VITE_API_URL=http://localhost:8080

# Google Maps JS API key (Maps + Marker). Required for the Eventos/Ubicación maps.
VITE_GOOGLE_MAPS_API_KEY=
